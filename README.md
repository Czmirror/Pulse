# Pulse

ブラウザで動くシンプルなリズムゲーム。
Bevy 0.15 + WebAssembly 製。PC・スマートフォン両対応。

## ゲームルール

- 画面中央の**ターゲットリング**に向かって外側から**パルス（円形の波）**が迫ってくる
- パルスがターゲットリングと重なった瞬間に**タップ / クリック**する
- 精度に応じてスコアが加算される：
  - **PERFECT** : +100 × コンボ倍率
  - **GOOD**    : +50 × コンボ倍率
  - **MISS**    : コンボリセット
- コンボが続くと倍率アップ（最大 ×4）
- 30 秒間のスコアを競う

## ビルド方法

```bash
# 依存ツールを含めて一括ビルド
./build_web.sh

# ローカルサーバで動作確認
python3 -m http.server 8080 --directory web
# ブラウザで http://localhost:8080 を開く
```

### 必要なもの

- Rust (stable, 1.80+)
- `wasm32-unknown-unknown` target（スクリプトが自動インストール）
- `wasm-bindgen-cli`（スクリプトが自動インストール）

## 技術スタック

| ツール | バージョン |
|---|---|
| Bevy | 0.15 |
| wasm-bindgen | 0.2 |
| Rust edition | 2021 |
